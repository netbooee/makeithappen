[build]
  command   = "npm run build"
  publish   = "dist"

[functions]
  node_bundler = "esbuild"

# Serve index.html for all routes so HashRouter handles navigation client-side
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
